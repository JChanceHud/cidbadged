{
  "name": "dnslink-cid",
  "version": "0.0.1",
  "description": "Serve an svg with a partial hash bound to a dns address",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chance Hudson",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.12.5",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "dnslink": "^0.2.0"
  }
}
