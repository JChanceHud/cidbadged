{
  "name": "cidbadged",
  "version": "0.0.4",
  "description": "http server for svg content identifier badges",
  "main": "build/index.js",
  "scripts": {
    "build": "rm -rf build || true && tsc",
    "deploy": "npm run build && now"
  },
  "author": "Chance Hudson",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.12.5",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "cidbadge": "^0.0.5",
    "dnslink": "^0.2.0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:JChanceHud/cidbadged.git"
  }
}
