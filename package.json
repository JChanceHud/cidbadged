{
  "name": "dnslink-cid-badge",
  "version": "0.0.1",
  "description": "Serve an svg with a partial hash bound to a dns address",
  "main": "build/index.js",
  "scripts": {
    "build": "rm -rf build || true && tsc",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chance Hudson",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.12.5",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "dnslink": "^0.2.0"
  }
}
